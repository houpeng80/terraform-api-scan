info:
  version: v1.35.0
  title: resource_huaweicloud_vpc_subnet
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: vpc
paths:
  /v2.0/subnets/{id}/tags/action:
    post:
      tag: vpc
      operationId: Create
  /v2.0/subnets/{id}/tags:
    get:
      tag: vpc
      operationId: Get
  /v1/{project_id}/subnets/{id}:
    get:
      tag: vpc
      operationId: Get
  /v1/{project_id}/subnets:
    post:
      tag: vpc
      operationId: Create
  /v1/{project_id}/vpcs/{vpcid}/subnets/{id}:
    delete:
      tag: vpc
      operationId: Delete
    put:
      tag: vpc
      operationId: Update
