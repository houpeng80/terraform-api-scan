info:
  version: v1.35.0
  title: resource_huaweicloud_dws_cluster
  description: 404.This resource is waiting to be upgraded, so there is none method output.
schemes:
  - https
host: huaweicloud.com
tags:
  - name: dws
paths:
  /v1.0/{project_id}/clusters/{clusterId}/reset-password:
    post:
      tag: dws
      operationId: ResetPassword
  /v1.0/{project_id}/clusters/{clusterId}/resize:
    post:
      tag: dws
      operationId: Resize
  /v1.0/{project_id}/clusters/{id}:
    delete:
      tag: dws
      operationId: Delete
    get:
      tag: dws
      operationId: Get
  /v1.0/{project_id}/clusters:
    post:
      tag: dws
      operationId: Create
