info:
  version: v1.35.0
  title: resource_huaweicloud_scm_certificate
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: scm
paths:
  /v3/scm/certificates/import:
    post:
      tag: scm
      operationId: Import
  /v3/scm/certificates/{id}/push:
    post:
      tag: scm
      operationId: Push
  /v3/scm/certificates/{id}:
    delete:
      tag: scm
      operationId: Delete
    get:
      tag: scm
      operationId: Get
