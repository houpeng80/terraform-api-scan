info:
  version: v1.35.0
  title: resource_huaweicloud_networking_secgroup
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: vpc
paths:
  /v2.0/security-groups/{id}:
    put:
      tag: vpc
      operationId: Update
  /v3/{project_id}/vpc/security-group-rules/{ruleId}:
    delete:
      tag: vpc
      operationId: Delete
  /v3/{project_id}/vpc/security-groups/{secgroupId}:
    get:
      tag: vpc
      operationId: Get
    put:
      tag: vpc
      operationId: Update
  /v3/{project_id}/vpc/security-groups:
    post:
      tag: vpc
      operationId: Create
  /v1/{project_id}/security-group-rules/{ruleId}:
    delete:
      tag: vpc
      operationId: Delete
  /v1/{project_id}/security-groups/{securityGroupId}:
    delete:
      tag: vpc
      operationId: Delete
    get:
      tag: vpc
      operationId: Get
  /v1/{project_id}/security-groups:
    post:
      tag: vpc
      operationId: Create
