info:
  version: v1.35.0
  title: resource_huaweicloud_networking_port
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: vpc
paths:
  /v2.0/ports/{id}:
    delete:
      tag: vpc
      operationId: Delete
    get:
      tag: vpc
      operationId: Get
    put:
      tag: vpc
      operationId: Update
  /v2.0/ports:
    post:
      tag: vpc
      operationId: Create
