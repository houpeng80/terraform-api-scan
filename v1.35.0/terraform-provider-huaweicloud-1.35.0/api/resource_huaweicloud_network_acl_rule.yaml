info:
  version: v1.35.0
  title: resource_huaweicloud_network_acl_rule
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: vpc
paths:
  /v2.0/fwaas/firewall_policies/{id}/remove_rule:
    put:
      tag: vpc
      operationId: RemoveRule
  /v2.0/fwaas/firewall_rules/{id}:
    delete:
      tag: vpc
      operationId: Delete
    get:
      tag: vpc
      operationId: Get
    put:
      tag: vpc
      operationId: Update
  /v2.0/fwaas/firewall_rules:
    post:
      tag: vpc
      operationId: Create
