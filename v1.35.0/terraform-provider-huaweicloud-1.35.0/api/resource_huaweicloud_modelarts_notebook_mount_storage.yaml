info:
  version: v1.35.0
  title: resource_huaweicloud_modelarts_notebook_mount_storage
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: modelarts
paths:
  /v1/{project_id}/notebooks/{id}/storage/{storageId}:
    delete:
      tag: modelarts
      operationId: DeleteMount
    get:
      tag: modelarts
      operationId: GetMount
  /v1/{project_id}/notebooks/{id}/storage:
    post:
      tag: modelarts
      operationId: Mount
  /v1/{project_id}/notebooks/{id}:
    get:
      tag: modelarts
      operationId: Get
