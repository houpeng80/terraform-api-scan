info:
  version: v1.35.0
  title: resource_huaweicloud_modelarts_notebook
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: modelarts
paths:
  /v1/{project_id}/notebooks/{id}/start:
    post:
      tag: modelarts
      operationId: Start
  /v1/{project_id}/notebooks/{id}/stop:
    post:
      tag: modelarts
      operationId: Stop
  /v1/{project_id}/notebooks/{id}/storage:
    get:
      tag: modelarts
      operationId: ListMounts
  /v1/{project_id}/notebooks/{id}:
    delete:
      tag: modelarts
      operationId: Delete
    get:
      tag: modelarts
      operationId: Get
    put:
      tag: modelarts
      operationId: Update
  /v1/{project_id}/notebooks:
    post:
      tag: modelarts
      operationId: Create
