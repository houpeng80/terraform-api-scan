info:
  version: v1.35.0
  title: resource_huaweicloud_apig_vpc_channel
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: ecs
  - name: apig
paths:
  /v1/{project_id}/cloudservers/{serverID}:
    get:
      tag: ecs
      operationId: Get
  /v2/{project_id}/apigw/instances/{instanceId}/vpc-channels/{chanId}:
    delete:
      tag: apig
      operationId: Delete
    get:
      tag: apig
      operationId: Get
    put:
      tag: apig
      operationId: Update
  /v2/{project_id}/apigw/instances/{instanceId}/vpc-channels:
    get:
      tag: apig
      operationId: List
    post:
      tag: apig
      operationId: Create
