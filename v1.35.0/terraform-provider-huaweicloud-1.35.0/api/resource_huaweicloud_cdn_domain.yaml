info:
  version: v1.35.0
  title: resource_huaweicloud_cdn_domain
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: cdn
paths:
  /v1.0/cdn/domains/{domainId}/detail:
    get:
      tag: cdn
      operationId: Get
  /v1.0/cdn/domains/{domainId}/disable:
    put:
      tag: cdn
      operationId: Disable
  /v1.0/cdn/domains/{domainId}/origin:
    put:
      tag: cdn
      operationId: Origin
  /v1.0/cdn/domains/{domainId}:
    delete:
      tag: cdn
      operationId: Delete
  /v1.0/cdn/domains:
    post:
      tag: cdn
      operationId: Create
