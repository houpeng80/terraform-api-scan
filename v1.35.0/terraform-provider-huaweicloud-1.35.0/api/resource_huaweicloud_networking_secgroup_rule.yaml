info:
  version: v1.35.0
  title: resource_huaweicloud_networking_secgroup_rule
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: vpc
paths:
  /v3/{project_id}/vpc/security-group-rules/{ruleId}:
    get:
      tag: vpc
      operationId: Get
  /v3/{project_id}/vpc/security-group-rules:
    post:
      tag: vpc
      operationId: Create
  /v1/{project_id}/security-group-rules/{ruleId}:
    delete:
      tag: vpc
      operationId: Delete
    get:
      tag: vpc
      operationId: Get
  /v1/{project_id}/security-group-rules:
    post:
      tag: vpc
      operationId: Create
