info:
  version: v1.35.0
  title: resource_huaweicloud_iec_server
  description: 
schemes:
  - https
host: huaweicloud.com
tags:
  - name: iecs
paths:
  /v1/cloudservers/delete:
    post:
      tag: iecs
      operationId: DeleteServers
  /v1/cloudservers/{serverID}:
    get:
      tag: iecs
      operationId: GetServer
    put:
      tag: iecs
      operationId: UpdateServer
  /v1/cloudservers:
    post:
      tag: iecs
      operationId: CreateServer
  /v1/cloudvolumes/{CloudVolumeID}:
    get:
      tag: iecs
      operationId: Get
